command = direct:/usr/bin/zsh

# Font Configuration
font-size = 12

# Window Configuration
window-decoration = false
window-padding-x = 12
window-padding-y = 12
background-opacity = 0.6 
background-blur-radius = 32

# --- Animated Cursor Configuration ---
# Point to the absolute path of the shader you cloned
custom-shader = "/home/omarh/ghostty-shaders/cursor_blaze.glsl"
custom-shader-animation = true

# Essential: shell-integration must NOT have 'no-cursor' for animations to work
shell-integration = detect
shell-integration-features = cursor,sudo,title

cursor-style = block
cursor-style-blink = false  
# Most shaders look better with blinking off
# --------------------------------------

# Scrollback
scrollback-limit = 3023

# Terminal features
mouse-hide-while-typing = true
copy-on-select = false
confirm-close-surface = false

# Disable annoying notifications
app-notifications = no-clipboard-copy,no-config-reload

# Key bindings
keybind = ctrl+shift+n=new_window
keybind = ctrl+t=new_tab
keybind = ctrl+plus=increase_font_size:1
keybind = ctrl+minus=decrease_font_size:1
keybind = ctrl+zero=reset_font_size
keybind = shift+enter=text:\n

# Material 3 UI elements
unfocused-split-opacity = 0.7
unfocused-split-fill = #44464f

# Other
gtk-titlebar = false
gtk-single-instance = true
config-file = theme

